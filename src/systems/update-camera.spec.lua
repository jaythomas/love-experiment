describe('systems/update-camera', function()

  local system = require 'src/systems/update-camera'

  describe('it should be a function', function()
    assert.equal('function', type(system))
  end)

end)
